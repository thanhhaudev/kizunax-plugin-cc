# Test fixtures

Tree-sitter grammar `.wasm` files used by `internal/symbols/treesitter`
tests. Pulled on demand; not committed (per-grammar licenses vary, blobs
are large, and updates should be deliberate).

Run before testing:

    ./test-fixtures/fetch.sh
